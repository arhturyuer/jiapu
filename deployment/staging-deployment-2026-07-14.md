# 预发布部署记录（2026-07-14）

## 环境与授权

- 环境：`cloud1-d5gs5yj4l283d9c6d`，上海，个人免费体验版。
- 当前角色：`staging`。经用户确认，验收、升级套餐和发布审批完成后才允许原地转为生产。
- 微信公众平台登记主体：赵东明。
- 初始运营账号：`metrozhaox@qq.com`，CloudBase Auth 用户和 `super_admin` 白名单均已创建；密码不进入仓库或部署记录。

## 变更前保护与初始化

- 已导出 20 个集合的变更前本地安全快照和云存储对象清单，文件位于被 `.gitignore` 排除的 `deployment/backups/`。
- 清理前共 26 条历史/样例记录，云存储对象为 0；按已锁定的“不迁移旧业务数据”决策清空后重新初始化。
- schema v3 已写入：单谱 500 人、归档 30 天、注销冷静期 7 天。
- 当前回读：1 个预发布微信测试用户、0 个家谱、1 个有效 `super_admin`。测试用户由预览环境产生，不是迁移数据。

## 云端部署结果

- 三个新函数均为 `Nodejs20.19`、512 MB、状态 Available：
  - `youpuUserApi`：15 秒。
  - `youpuOpsApi`：20 秒。
  - `youpuJobs`：60 秒，两个启用的定时触发器。
- 后台维护密钥已在 bootstrap 后轮换；验证工具只输出环境变量键，不输出值。
- 17 个业务集合共创建并回读 57 个索引；20 个集合均为 `ADMINONLY`。
- 云函数自定义规则已回读：用户/运营 API 仅非匿名登录用户可调用，内部任务禁止客户端调用，旧函数仅在切换观察期保留。
- CloudBase Auth 已回读：仅邮箱登录开启；匿名、用户名、手机登录关闭；首次登录强制改密。
- 运营后台已部署到 `https://cloud1-d5gs5yj4l283d9c6d-1453718878.tcloudbaseapp.com`，首页和 JS 资源均返回 HTTP 200。

## 已通过的冒烟检查

- 无微信身份调用用户 API 返回 `UNAUTHENTICATED`。
- 无 Auth 身份调用运营 API 返回 `UNAUTHENTICATED`。
- 错误维护密钥调用内部任务返回 `UNAUTHORIZED`。
- 最终 preflight 通过：15/15 测试（含 500 人图谱）、管理端 TypeScript 检查和生产构建全部成功。
- 微信 CLI 预览成功，基础库 3.16.2、`urlCheck: true`，预览包 153.6 KB。

## 免费套餐降级与正式发布阻断项

CloudBase 明确返回 `OperationDenied.FreePackageDenied`，当前套餐不能设置云存储 `CUSTOM` 规则。预发布暂时回读为 `PRIVATE`（仅创建者和管理员可读写），可用于功能联调，但不满足“客户端不可直读原图”的正式发布要求。升级套餐后必须：

1. 不设置 `ALLOW_PRIVATE_STORAGE_FALLBACK`，重新运行 `deployment/apply-security.mjs`，并回读为 `CUSTOM`。
2. 在 CloudBase 控制台给初始运营邮箱重设密码，完成首次改密和八个后台模块的浏览器验收。
3. 配置并实测图片异步审核回调、暂存目录 24 小时生命周期，以及通过/疑似/违规三类图片。
4. 完成 500 人真机、三角色、20 个并发接受邀请、弱网、隐私授权、注销冷静期和体验版端到端验收。
5. 配置错误率、P95、审核积压、资源 80% 和注销失败告警，并完成真实备份恢复演练与 30 天保留方案。
6. 完成备案、主体认证、服务类目、隐私指引、客服、审核说明；之后才可切换 `production`、上传 `v1.0.0` 和提交微信审核。

本记录不代表已正式发布，不创建 `v1.0.0` 标签。
